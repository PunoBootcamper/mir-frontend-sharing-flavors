import { Recipe } from "../../interfaces";
const RecipeCardDetailed = (recipe: Partial<Recipe>) => {
  console.log(recipe);
  return (
    <div>
      <h1>hola</h1>
    </div>
  );
};

export default RecipeCardDetailed;
